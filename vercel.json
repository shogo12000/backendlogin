{
    "version": 2,
    "builds": [
      {
        "src": "server.js",   // O arquivo principal do seu backend
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/server.js"
      }
    ]
  }